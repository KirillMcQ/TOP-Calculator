// Arithmetic functions
const add = (x, y) => {
    return x + y;
}

const subtract = (x, y) => {
    return x - y;
}

const multiply = (x, y) => {
    return x * y;
}

const divide = (x, y) => {
    return x / y;
}

// Variables needed for calculations; will be user inputted
let firstNumber = "";
let operator = "";
let secondNumber = "";

